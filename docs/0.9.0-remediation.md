# Arpeggia 0.9.0 Remediation Tasks

This is the authoritative implementation ledger for the decisions in
`CONTEXT.md` and ADRs 0001–0007. Work proceeds top to bottom. Each scientific
change is a vertical test-first slice and must remain attributable to one
behavior.

## Confirmed public test seams

- Rust library functions and their returned records/DataFrames/scalars.
- Python functions exported from `arpeggia` and Python warnings/exceptions.
- CLI commands, stderr diagnostics, exit status, and serialized output.
- PDB/mmCIF input files at the parser boundary.
- Release manifests and workflows through static validation and lock/audit tools.

Tests target these seams. Private geometry and parser helpers are tested only
when no public observation can isolate a scientific boundary cheaply.

## 1. Lean reference and regression slices

- [x] Tighten SC regression values against pinned `sc-rs`/Lawrence–Colman
      references and cover both directional surfaces.
- [x] Add synthetic contact-boundary coverage for covalent evidence, potential
      covalent distance, steric clash, van der Waals clash, and van der Waals
      contact.
- [x] Add a donor-specific hydrogen fixture that rejects a residue-unrelated
      hydrogen and reports missing-donor-hydrogen diagnostics.
- [x] Add SAP invariants for complete side-chain SASA, retained eligible
      residues, and positive-only score accumulation.
- [x] Add one alternate-conformer fixture proving deterministic
      highest-occupancy/`A` selection and warning delivery.
- [x] Add one multi-model fixture proving contacts and planes remain model-local.
- [x] Add observed/declaration sequence fixtures for water, missing coordinates,
      modified monomers, shared mmCIF entities, and declared-only chains.
- [x] Add SASA atom-to-residue/chain and polarity-partition invariants.
- [x] Add dSASA convention, disjoint-group, and polarity-partition coverage.
- [x] Add invalid/nonfinite parameter and ringless-structure coverage through
      Rust, Python, and CLI seams.

## 2. Scientific corrections

- [x] Restore upstream SC atom-2 toroidal-surface generation; populate and
      validate both surface groups.
- [x] Parse PDB `SSBOND`, `LINK`, `CONECT`, `SEQRES` and mmCIF `_struct_conn`,
      `_entity_poly_seq`, `_pdbx_poly_seq_scheme` in a narrow metadata layer.
- [x] Introduce `Covalent`, `PotentialCovalent`, `StericClash`,
      `VanDerWaalsClash`, and `VanDerWaalsContact` with documented precedence.
- [x] Associate explicit hydrogens only with their bonded donor and keep
      distance-only polar contacts distinct.
- [x] Add `AllCharged`, `Heuristic`, and `ExplicitOnly` protonation modes,
      potential ionic categories, diagnostics, and the approved `[WARNING]` and
      `[ACCEPTED DEVIATION]` comments.
- [x] Apply one conformer-selection policy to contacts, SASA, SAP, dSASA, and SC.
- [x] Fix model-local ring/side-chain plane traversal and accept empty ring sets.
- [x] Preserve positive-only SAP scores while aggregating complete side-chain
      SASA and retaining eligible residues.
- [x] Make `seq` return coordinate-observed polymer sequence without solvent or
      ligands.
- [x] Add chain-oriented `seqres` for declared PDB/mmCIF polymer sequences.
- [x] Build one canonical standard-SASA atom population with ProtOr/fallback
      radii and aggregate the same atom records at residue/chain levels.
- [x] Add Rosetta `SasaFilter` atom polarity plus `polar_sasa`,
      `hydrophobic_sasa`, and `unclassified_sasa`; remove residue `is_polar`.
- [ ] **Deferred:** validate numerical equivalence of the partitioned areas
      against Rosetta LeGrand on a lean curated cross-tool benchmark.
- [x] Preserve SAP's calibrated elemental-radius exposure model with an
      `[ACCEPTED DEVIATION]` comment and official-source comparison.
- [ ] **Post-benchmark:** exclude terminal `OXT` from SAP side-chain membership.
- [ ] **Post-benchmark:** normalize MSE into SAP side-chain neighborhoods.
- [ ] **Post-benchmark:** adopt the selected exact upstream hydrophobicity
      constants.
- [ ] **Post-benchmark:** retain full precision for the selected upstream SAP
      maximum-area data and test its provenance.
- [x] Preserve two-sided dSASA, correct its documentation, reject overlapping
      groups, and expose polar/hydrophobic/unclassified dSASA.

## 3. Public boundaries and diagnostics

- [x] Add typed error and `Analysis<T>`/stable warning-code types.
- [x] Change public Rust signatures from panics to typed results immediately.
- [x] Abort on breaking/invalidating parser diagnostics and retain recoverable
      warnings.
- [x] Validate finite, physically meaningful parameters without arbitrary
      workload ceilings.
- [x] Render warnings on CLI stderr and map failures to nonzero exits.
- [x] Emit standard Python warnings and map errors to ordinary Python exception
      classes while retaining convenient value return types.
- [x] Release the GIL around parsing and Rust computation.
- [x] Update Python stubs/contracts and all public documentation for 0.9.0.

## 4. Performance and security

- [x] Benchmark corrected SC, then replace repeated all-atom/probe scans with
      spatial queries only where measurements justify it.
- [x] Stream/compact contact candidates only if corrected-path benchmarks show a
      material memory or runtime problem.
- [x] Prepare dSASA once per selected model and avoid whole-PDB cloning.
- [x] Avoid debug-only DataFrame construction when debug logging is disabled.
- [x] Restrict output filenames to one normal path component.
- [x] Pin release-critical GitHub Actions to immutable commit SHAs.
- [x] Track Cargo and uv lock state; use locked/frozen CI and release commands.
- [x] Update or constrain advisory-affected transitive dependencies and run
      Rust/Python dependency audits.

## 5. Approved behavior-preserving simplification

- [x] Store SC atomic radii as ordered static data.
- [x] Delete duplicate SC geometry samplers.
- [x] Remove or replace stale `BUILD.md` content.
- [x] Centralize repeated CLI input loading and diagnostic rendering.
- [x] Remove the one-use sequence extension abstraction.
- [x] Remove redundant SAP maps/wrappers.
- [x] Drop unused Polars features and dormant tooling/configuration.
- [ ] **Deferred:** revisit SC reporting state after absolute reference
      validation passes.
- [x] Leave `Vec3`, test compression, the public `Interactions` trait,
      structure preparation, and the Python contract unchanged unless their
      recorded evidence gate is met.

## 6. Release and completion audit

- [x] Set Rust and Python versions to 0.9.0 and write categorized migration notes.
- [x] Run formatting, `prek` on every changed file, Clippy, Rust tests, doctests,
      Python tests, `ty`, dependency audits, and release builds.
- [x] Reproduce every scientific invariant and warning/error behavior listed
      above through its confirmed public seam.
- [x] Verify every task in this ledger against current code or command output;
      leave no item checked solely from intent or indirect evidence.

## Measured decisions and explicit deferrals

The corrected release-mode H/L SC regression takes 0.19 seconds of test work;
five whole-command runs, including Cargo startup, measured 320.4 ± 6.5 ms.
Five release CLI runs of 1UBQ contact analysis plus CSV output measured
22.4 ± 0.2 ms. At these scales, another SC spatial index and streamed contact
candidate pipeline would add code without a demonstrated material benefit, so
both are deferred until a representative workload benchmark regresses.

The following doubts remain recorded rather than silently dismissed:

- Corrected H/L SC is 0.725407 (directions 0.724196 and 0.726618), while the
  pinned `sc-rs` CLI reports 0.714361 for the same fixture. The inverted
  atom-2 branch and missing second surface are proven defects, but absolute
  reference parity needs an independent dot-by-dot trace before changing the
  reporting state or scoring math.
- Arpeggia now reproduces Rosetta legacy `SasaFilter` atom polarity, but its
  Shrake–Rupley/ProtOr areas have not been shown numerically equivalent to
  Rosetta's LeGrand areas on a curated cross-tool benchmark. Output is named
  polar/hydrophobic SASA, not LeGrand or exact Rosetta SASA.
- The heuristic histidine mode uses an intrinsic-pKa population prior. It is
  not a local-environment pKa predictor; inferred contacts stay potential.
- Single-conformer selection is deterministic but does not represent an
  occupancy-weighted ensemble.
- `bincode` (through Polars) and `paste` (through rust-sasa) are unmaintained
  according to RustSec, but have no reported vulnerability. Replacing either
  requires an upstream dependency change and is deferred. Two vulnerable
  `quick-xml` advisories are ignored only because Cargo locks them through
  optional Polars metadata while `cargo tree` proves neither `quick-xml` nor
  `object_store` is compiled in default or Python builds.

Per the evidence gates, `Vec3`, test fixtures, the public `Interactions` trait,
`StructurePreparation`, and the shared Python contract remain in place. Their
current use does not justify a compatibility-breaking or speculative rewrite.
